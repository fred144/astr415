#include <stdio.h> // libraries that are standard in C

double x = 12;

int main(){ // all programs should contain a maing function
    // returns an argument that is of the int type. 
    // \n is new line
    // finish any command line with a semi colon
    // indent no required 
    // 
    printf("Hello World!\n");
    
    return 0;
}

// to compile use:
// gcc -O2 -o executable_name myprog.c -lm 